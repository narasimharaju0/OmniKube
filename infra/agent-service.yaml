apiVersion: v1
kind: Service
metadata:
  name: omnikube-agent-service
  labels:
    app: omnikube-agent
spec:
  type: ClusterIP
  selector:
    app: omnikube-agent
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
