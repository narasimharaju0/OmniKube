name: Deploy

on:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  build-and-validate:
    name: Build and Validate
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./server

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: server/requirements.txt

      - name: Install Python dependencies
        run: pip install -r requirements.txt

      - name: Validate and smoke-test server application
        env:
          STRIPE_SECRET_KEY: "mock_key_for_ci"
          STRIPE_WEBHOOK_SECRET: "mock_secret_for_ci"
          LOG_LEVEL: "DEBUG"
          OMNIKUBE_DATA_DIR: "./.ci-data"
        run: |
          python -m compileall -q .
          python -c "import app; print('App smoke test passed successfully!')"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build production image (placeholder)
        uses: docker/build-push-action@v6
        with:
          context: ./server
          file: ./Dockerfile.prod
          push: false
          tags: omnikube-server:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
